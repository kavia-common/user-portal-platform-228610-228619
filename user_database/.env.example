# MySQL database container (documentation only)
# The startup.sh currently provisions MySQL with defaults, but application containers
# should use these normalized variables for connectivity.

DB_HOST=localhost
DB_PORT=5001
DB_USER=appuser
DB_PASSWORD=change_me
DB_NAME=myapp
